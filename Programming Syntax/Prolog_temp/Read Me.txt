Prolog is a logic based language, you only tell it what's true, never what's false
the '.' ends each line (like the ';' in c and java)
use ';' to continue asking a question in query mode
'gtrace' for tracing

run/compile:
	open command line 'swipl'
	load file by "['filename']."
