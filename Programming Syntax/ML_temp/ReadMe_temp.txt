ML is a functional programming language, but can be non-functional when neccassary
'-' is an operator ( 1 - 2 )[takes two arguments], not the negative sign... use '~' for negative numbers
the last evalutated value is stored in a special variable 'it'

to run/load sorce code
	run sml
	use command:
		use "filepath";
	refer to ML\bin\smlnj.pdf
